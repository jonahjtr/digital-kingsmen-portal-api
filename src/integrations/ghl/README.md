# GoHighLevel Integration (Future)

This directory is reserved for future GHL integration.

## Planned hooks

- `onProjectCreated(project)` — sync new projects to GHL opportunities
- `onClientRequestSubmitted(request)` — create GHL tasks or notes
- `onApprovalStatusChanged(approval)` — notify assigned rep

## Interface stub

See `client.ts` for the `GhlClient` interface. No implementation is wired yet.
